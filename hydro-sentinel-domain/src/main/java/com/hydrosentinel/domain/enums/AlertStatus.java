package com.hydrosentinel.domain.enums;

/**
 * Lifecycle status of an alert.
 */
public enum AlertStatus {
    /** Alert is currently active and unresolved. */
    ACTIVE,
    /** Alert has been acknowledged and resolved. */
    RESOLVED
}
